# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'resume-ios' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for resume-ios
  pod 'Alamofire'
  pod 'SDWebImage'
  pod 'SnapKit'

  target 'resume-iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'resume-iosUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
